import { Main } from './core/Main.js';

const app = document.getElementById('app');
// create instance of the ui exist in the -> main core (when make instance it will render ui direcly )
new Main(app);
